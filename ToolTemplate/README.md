# Tool Name Here

One-sentence description of what this tool does.

## What It Does

Describe the UEFN workflow problem this tool solves and how it works.

## Requirements

- Python 3.10+
- List any extra dependencies here (and in `requirements.txt`)

## Usage

```
python tool_template.py --example-option value
```

| Option | Type | Default | Description |
|---|---|---|---|
| `--example-option` | str | `"default"` | Replace with your actual options |

## Setup

1. Copy the `ToolTemplate/` folder and rename it to match your tool name
2. Rename `tool_template.py` and update the docstring, options, and logic
3. Fill in this README (keep the section structure)
4. Add a `requirements.txt` if your tool has dependencies beyond the standard library

## Example

Show a real invocation and its output.
